import React from "react";

function Avatar(props) {
    const {
        url,
        small,
        medium,

    } = props

    return (
        <div>

        </div>
    );
}

export default Avatar;
